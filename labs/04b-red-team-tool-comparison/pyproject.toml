[project]
name = "red-team-tool-comparison"
version = "0.1.0"
description = "Common corpus and result normalization for LLM red-team comparisons"
requires-python = ">=3.10"

[tool.pytest.ini_options]
testpaths = ["tests"]
